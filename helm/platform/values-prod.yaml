backend:
  replicaCount: 3
  image:
    repository: ghcr.io/yourorg/store-backend
    tag: v1.0.0
    pullPolicy: Always
  ingress:
    host: api.yourdomain.com
    tls:
      enabled: true
      secretName: api-tls
